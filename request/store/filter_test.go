package store

import (
	"testing"
)

func byId(i int) *int {

	return &i

}

func TestXxx(t *testing.T) {

	// q := `SELECT * FROM request `
	// buf := bytes.NewBufferString(q)

	// data := map[string]any{}

	// fby := request.FilterBy{ProjectId: byId(1)}
	// applyFilter(fby, data, buf)

	// fmt.Printf("buf string is %s ", buf.String())

}
